export type PostsType = {
    userId: number,
    id: number,
    title: string,
    body: string
}

export type PhotoType = {
    albumId: number,
    id: number,
    title: string,
    url: string,
    thumbnailUrl: string
}
